window starting width=1400; integer
window starting height=1000; integer
map export format=png; can take bmp, tga, png, or jpeg as an argument. Anything else will default to tga.
battle scale circle scale factor=0.075
battle scale circle color=(255, 128, 0); RGB values, integers only
battle scale circle transparency=60; 0-255, transparent-opaque
background color=(10, 25, 64); RGB values, integers only
enable random battle displacement=no; yes or no, if yes offsets the center of each battle by a small random amount to make sure they don't overlay. Position is not consistent between window instances.